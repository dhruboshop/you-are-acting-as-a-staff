# Docker Setup

Docker is optional. It is not required for local development.

Use it only if your machine can comfortably run containers or if you want to test the deployment image.

```sh
docker compose up --build
```

The API reads the same `.env` file as normal local development.
