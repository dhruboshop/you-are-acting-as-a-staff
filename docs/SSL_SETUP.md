# SSL Setup

Render automatically provisions SSL for custom domains after DNS is correct.

## Check

1. Open `https://api.yourdomain.com/health`.
2. Confirm the browser shows a lock icon.
3. Use HTTPS in `API_BASE_URL`.

Do not use cleartext HTTP for production APKs.
