services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped

# Docker is optional. Local development is intentionally supported without containers:
# cd backend && npm install && npm run dev
