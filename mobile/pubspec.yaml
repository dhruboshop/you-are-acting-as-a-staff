name: retail_whatsapp_loyalty
description: Retail WhatsApp loyalty platform for local shops.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.0
  google_sign_in: ^6.2.2
  http: ^1.2.2
  qr_flutter: ^4.1.0
  share_plus: ^10.1.2
  path_provider: ^2.1.5
  csv: ^6.0.0
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
